\ProvidesPackage{abc}

% common packages
\usepackage{mathtools}
\usepackage{epigraph}
\usepackage[russian]{babel}
\usepackage{hyperref}
\usepackage{amsmath,amsfonts,amsthm,amssymb}
\usepackage{xcolor}

% Theorems, lemms, definitions...
\newtheorem*{theorem*}{Теорема}
\newtheorem*{lemma*}{Лемма}
\newtheorem{theorem}{Теорема}[section]
\newtheorem{corl}{Следствие}[section]
\newtheorem{claim}{Утверждение}[section]
\newtheorem{defn}{Определение}[section]
\newtheorem{lemma}[theorem]{Лемма}
\newtheorem{exmpl}{Пример}[section]

% common defines
\newcommand{\KK}{\mathbb{K}}
\newcommand{\NN}{\mathbb{N}}
\newcommand{\ZZ}{\mathbb{Z}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\CC}{\mathbb{C}}
\newcommand{\PP}{\mathbb{P}}
\newcommand{\EE}{\mathbb{E}}
\newcommand{\DD}{\mathbb{D}}

\newcommand{\AL}{\mathcal{A}}
\newcommand{\BL}{\mathcal{B}}
\newcommand{\IL}{\mathcal{I}}
\newcommand{\NL}{\mathcal{N}}

\newcommand{\BK}{\mathfrak{B}}
\newcommand{\SK}{\mathfrak{S}}

\newcommand{\al}{\alpha}
\newcommand{\bb}{\beta}
\newcommand{\eps}{\varepsilon}
\newcommand{\sgm}{\sigma}
\newcommand{\om}{\omega}

\newcommand{\Om}{\Omega}

\newcommand{\la}{\langle}
\newcommand{\ra}{\rangle}

\newcommand{\dd}{\partial}

\DeclarePairedDelimiter\norm{\lVert}{\rVert}%
\DeclarePairedDelimiter\ceil{\lceil}{\rceil}%
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}%
\DeclarePairedDelimiter\round{[}{]}%

\DeclareMathOperator{\otherwise}{otherwise}
\DeclareMathOperator{\mex}{mex}
\DeclareMathOperator{\ravel}{ravel}
\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\conv}{conv}
\DeclareMathOperator{\cov}{cov}
\DeclareMathOperator{\const}{const}
\DeclareMathOperator{\dom}{dom}
\DeclareMathOperator{\for}{for}
\DeclareMathOperator{\extr}{extr}

\newcommand{\holds}{\hookrightarrow}
\newcommand{\toto}{\rightrightarrows}
\newcommand{\topto}[1]{\overset{#1}{\to}}
\newcommand{\botto}[1]{\underset{#1}{\to}}
\newcommand{\toptoto}[1]{\overset{#1}{\toto}}
\newcommand{\bigtimesRelay}[1][]{\overset{#1}{\underset{\bottom}{\times}}}
\newcommand{\bigtimes}[1][]{
    \def\bottom{{#1}}
    \bigtimesRelay
}

% fancy page style
\usepackage{kvoptions} 
\SetupKeyvalOptions{prefix=ML@}
\DeclareStringOption[Default title]{mytitle}
\DeclareStringOption[white]{mycolor}
\ProcessKeyvalOptions*

\usepackage{fancybox,fancyhdr}
\fancyhead[R]{\colorbox{black}{\color{\ML@mycolor}{\textbf{\large \thepage}}}}  %% odd-right 
\fancyhead[L]{\colorbox{black}{\textbf{\thesection}}}% odd-left
\fancyhead[C]{\ML@mytitle}
\fancyfoot[C,L,R]{}

% narrow margins, so no one would put here their marvellous proof
\usepackage[b5paper,left=10mm,right=10mm, top=17mm,bottom=15mm,bindingoffset=0cm]{geometry}

\definecolor{urlcolor}{HTML}{0645AD} % цвет гиперссылок
\hypersetup{pdfstartview=FitH, linkcolor=black,urlcolor=urlcolor, colorlinks=true}

\pagestyle{fancy}


\usepackage{listings} %% собственно, это и есть пакет listings
\definecolor{mauve}{rgb}{0.58,0,0.82}
\definecolor{dkgreen}{rgb}{0,0.4,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{lightgray}{rgb}{0.95,0.95,0.95}

\def\commentstyle{\color{gray}}
\lstset{ %
extendedchars=\true,
  language=C++,                   % the language of the code
  basicstyle=\footnotesize\ttfamily, % the size of the fonts that are used for the code
  numbers=left,                   % where to put the line-numbers
  numberstyle=\footnotesize\color{black},  % the style that is used for the line-numbers
  stepnumber=1,                   % the step between two line-numbers. If it's 1, each line 
                                  % will be numbered
  numbersep=0.7em,                % how far the line-numbers are from the code
  backgroundcolor=\color{lightgray}, % choose the background color. You must add \usepackage{color}
  showspaces=false,               % show spaces adding particular underscores
  showstringspaces=false,         % underline spaces within strings
  showtabs=false,                 % show tabs within strings adding particular underscores
  frame=single,                   % adds a frame around the code
  rulecolor=\color{black},        % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
  tabsize=2,                      % sets default tabsize to 2 spaces
  breaklines=true,                % sets automatic line breaking
  breakatwhitespace=false,        % sets if automatic breaks should only happen at whitespace
  identifierstyle=\color{blue!25!black},  
  keywordstyle=\color{blue!90!black},      % keyword style
  commentstyle=\commentstyle,     % comment style
  stringstyle=\color{mauve},      % string literal style
  escapeinside={\`}{\`},          % if you want to add a comment within your code
  escapebegin=\commentstyle\footnotesize,
  %morekeywords={n,k},             % if you want to add more keywords to the set
  morecomment=[l][\color{dkgreen}]{\#}, % to color #include<cstdio> 
  morecomment=[s][\commentstyle\color{gray!50!black}]{/**}{*/},
  escapeinside=|,   % если нужно добавить комментарии в коде
keepspaces=true
}